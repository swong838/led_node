/**
 * seeing if we can adjust state from the frontend
*/


// import chalk from 'chalk';
// import * as readline from 'readline';


const testbed = (cb) => {
    setInterval(cb, 1000);
}

export default testbed;
